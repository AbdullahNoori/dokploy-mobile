// Ensure Unistyles is configured before any components render.
import "@/src/styles/unistyles";

// Hand off to Expo Router's default entry with no additional side effects.
import "expo-router/entry";
